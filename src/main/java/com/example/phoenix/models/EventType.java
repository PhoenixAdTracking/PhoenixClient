package com.example.phoenix.models;

/**
 * Enum for the various types of events that can be logged for an interaction.
 */
public enum EventType {

    /**
     * Enum value for when a user has clicked on an ad.
     */
    CLICK,

    /**
     * Enum value for when a user has purchased a product from an ad.
     */
    PURCHASE;
}
